## What & why

<!-- What does this change, and why? Link an issue if one exists. -->

## How was this tested?

<!-- Which Markdown did you run through the tool? Did you check the generated HTML in a browser (light + dark theme)? -->

## Checklist

- [ ] `pnpm test` passes
- [ ] No new runtime dependencies were added to the generated HTML output
- [ ] Tested against a real Markdown folder, not just a syntax check
- [ ] README updated if behavior or CLI flags changed
